# Worker report — T-002F shell follow-ups (sharp/LGPL out, exact eslint pin, W-01 header brand, README env)

- Author: worker-b
- Date: 2026-09-26
- Branch: `arena/01a0dc47-haleman`. This is the harness-pinned session branch, used as the **alias of
  `w-b/T-002F-shell-followups`** (D-S7). It was branched from `main` @ `90aa3a3`, which is the PR #5 merge, so the
  round-2 orders are included.
- Milestone: M1 (Foundations)
- Order: `reports/senior/M1-review-1.md` §Next orders → T-002F, items 1–4 plus the optional pre-hydration label fix

## Pre-flight (done before any file change)

```
$ git branch --show-current                  → arena/01a0dc47-haleman   (NOT worker-a's arena/01a0dc32-haleman)
$ git status                                 → nothing to commit, working tree clean
$ git fetch origin; git rev-list --left-right --count HEAD...origin/main   → 0  0   (already == origin/main 90aa3a3)
$ gh pr view 5 --json state,mergedAt         → MERGED 2026-09-26T05:41:02Z, mergeCommit 90aa3a3
$ gh pr list --state all                     → #6 T-001 (worker-a, open) · #5/#4/#3/#2/#1 merged · no T-002F / T-003B PR
$ git branch -r ; git log --all | grep -i "T-002F\|T-003B"   → only origin/main; no existing T-002F/T-003B work
```
The round-2 documents are present: `reports/senior/M1-review-1.md`, the round-2 orders in `STATUS.md`, and the
T-002F board row. I read everything on the `AGENTS.md` list: brief, wireframe W-01, technical-decisions, architecture,
implementation-plan, tasks, tests, and design-system.

## What I did (short)

1. **sharp/LGPL out of the runtime.** I set `images: { unoptimized: true }` and added
   `outputFileTracingExcludes: { "*": ["**/node_modules/sharp/**/*", "**/node_modules/@img/**/*"] }` to
   `next.config.ts`. `.next/standalone` drops from **65.0 MB to 16.9 MB** (−74 %). Shipped packages drop from
   **18 to 7**, all MIT/Apache-2.0, with **0 LGPL/GPL** files and **0 native `.node` binaries**. The standalone server
   still serves `/` and the fonts.
2. **`eslint` pinned exactly to `9.39.5`.** All 12 direct deps are now exact. The lockfile diff is one line, and
   `npm ci` runs clean.
3. **No app-header brand on `/`.** The header moved into a small client `AppHeader` that uses `usePathname()`. It
   leaves out the brand on `/` only. `<ThemeToggle/>` stays in the same spot, is reachable by keyboard, and is the
   first tab stop. Every other route keeps the brand. The served HTML is already correct before JS runs.
4. **`frontend/README.md`:** a new *Environment* entry documents the dev-only `ALLOWED_DEV_ORIGINS`.
5. **Optional fix, done:** the pre-hydration toggle label flash is gone. Both labels are rendered, and CSS picks one
   from `html[data-theme]`. There is before/after proof below.

The palette, tokens, copy, routes and infra files are unchanged.

## Files changed

| File | What |
|---|---|
| `frontend/next.config.ts` | +`images.unoptimized`, +`outputFileTracingExcludes` (item 1), with a comment |
| `frontend/package.json` | `"eslint": "^9.39.5"` → `"9.39.5"` (item 2) |
| `frontend/package-lock.json` | the same root-spec line only (item 2) |
| `frontend/components/AppHeader.tsx` | **new**: header with the brand hidden on `/` only (item 3) |
| `frontend/app/layout.tsx` | the inline `<header>` is replaced by `<AppHeader/>` (item 3, file owned by T-002F per the review) |
| `frontend/components/ThemeToggle.tsx` | label comes from CSS on `[data-theme]` (optional fix) |
| `frontend/README.md` | `ALLOWED_DEV_ORIGINS` documented (item 4) |
| `reports/worker-b/T-002F.md`, `reports/worker-b/img/t002f-*.png` | this report + 5 screenshots |
| `STATUS.md` | my T-002F board row + appended dated entries only |

Untouched: `Makefile`, `docker-compose*.yml`, `docker/*`, `Caddyfile`, `.env.example`, `backend/**`, `tech/**`,
`product/**`, `design/**`, `src/**`, `styles/tokens.css`, and T-003B's paths (`app/consent`, `app/terms`,
`app/chat`, `lib/api.ts`).

## How to verify

```bash
git checkout arena/01a0dc47-haleman && cd frontend
npm ci && npm run lint && npm run typecheck && npm run check:tokens && npm run build
du -sh .next/standalone && ls .next/standalone/node_modules     # 19M; no sharp, no @img
# runtime, laid out exactly like the Dockerfile runner stage:
cp -r .next/static .next/standalone/.next/static && cp -r public .next/standalone/public
(cd .next/standalone && PORT=3100 node server.js)                # open http://localhost:3100
```

## Test results (real outputs)

### Environment / Docker

- `node v22.22.3`, `npm 10.9.8`, Chromium 153.0.8010.0 (npm-shipped `@sparticuz/chromium` + `playwright-core`,
  installed in `/tmp` outside the repo per D-S10. Not a project dependency.)
- `docker version` → `/bin/bash: line 1: docker: command not found`; `docker compose version` → same.

### Gates (verbatim, fresh copy of the tree with no `node_modules`/`.next`)

```
$ npm ci --no-audit --no-fund
npm warn deprecated eslint@9.39.5: This version is no longer supported. Please see https://eslint.org/version-support for other options.
added 381 packages in 10s
exit=0
$ npm ls eslint --depth=0
haleman-web@0.1.0 /tmp/t002f/fresh/frontend
`-- eslint@9.39.5
$ npm run lint
> haleman-web@0.1.0 lint
> eslint .
exit=0
$ npm run typecheck
> haleman-web@0.1.0 typecheck
> tsc --noEmit
exit=0
$ npm run check:tokens
ok   light: token names/order == design-system list (15 tokens)
ok   light: palette.md exposes 14 hexes (got 14)
ok   light: tokens.css hexes == palette.md row-for-row
ok   light: --shadow == "0 10px 30px rgba(43, 53, 66, .08)"
ok   dark: token names/order == design-system list (15 tokens)
ok   dark: palette.md exposes 14 hexes (got 14)
ok   dark: tokens.css hexes == palette.md row-for-row
ok   dark: --shadow == "0 10px 30px rgba(0, 0, 0, .35)"
ok   tokens.css hex set == palette.md hex set (bidirectional, incl. duplicates)
ok   no hex literals outside styles/tokens.css (scanned 17 files)
OK: tokens 1:1 with palette.md, 17 files scanned, 0 stray hexes
exit=0
$ npm run build
▲ Next.js 16.3.6 (Turbopack)
✓ Running next.config.ts took 17ms
  Creating an optimized production build ...
✓ Compiled successfully in 8.0s
  Running TypeScript ...
  Finished TypeScript in 1792ms ...
  Collecting page data using 1 worker ...
✓ Generating static pages using 1 worker (3/3) in 133ms
  Finalizing page optimization ...
Route (app)
┌ ○ /
└ ○ /_not-found
○  (Static)  prerendered as static content
exit=0
```
The token gate now scans 17 files instead of 16 because of the new `AppHeader.tsx`. The `eslint@9.39.5` deprecation
warning is the known D-5 trade-off: `eslint-config-next@16.3.6`'s bundled react plugin crashes on ESLint 10.

### Item 1: standalone size before/after

Both builds were made in this session from the same toolchain. "Before" is untouched `main` @ `90aa3a3` in a
detached worktree.

| Measure | Before (`main`) | After (T-002F) | Δ |
|---|---|---|---|
| `du -sh .next/standalone` | `65M` | `19M` | |
| `du -sb .next/standalone` | 65 049 164 B | 16 891 486 B | **−48.16 MB (−74 %)** |
| runner payload (standalone + `.next/static` + `public`, as the Dockerfile copies it) | 66 024 022 B | 17 867 075 B | −48.16 MB |
| `.next/standalone/node_modules` | 12 dirs incl. `@img` **46M**, `sharp` 883K | 7 dirs: `@next @swc client-only next react react-dom styled-jsx` | |
| estimated web image (payload + node:20-alpine ≈130 MB, T-002 method) | ≈196 MB | **≈148 MB** | estimate only, see Docker below |

The sharp support packages (`detect-libc`, `semver`, `@emnapi/runtime`, `@img/colour`) are pruned automatically
because their only parent is now excluded.

### Item 1: license scan of what ships (`.next/standalone/node_modules`)

The scanner walks top-level, `@scoped` and nested `node_modules` and reads each `package.json` `license`.

Before (reproduces the senior's 18 packages):
```
@emnapi/runtime@1.11.3  MIT                 @swc/helpers@0.5.23  Apache-2.0
@img/colour@1.1.0       MIT                 client-only@0.0.1    MIT
@img/sharp-libvips-linux-x64@1.3.3      LGPL-3.0-or-later      detect-libc@2.1.2  Apache-2.0
@img/sharp-libvips-linuxmusl-x64@1.3.3  LGPL-3.0-or-later      next@16.3.6        MIT
@img/sharp-linux-x64@0.35.4             Apache-2.0             react-dom@19.3.0   MIT
@img/sharp-linuxmusl-x64@0.35.4         Apache-2.0             react@19.3.0       MIT
@img/sharp-wasm32@0.35.4  Apache-2.0 AND LGPL-3.0-or-later AND MIT                semver@6.3.1 / 7.8.5  ISC
@next/env@16.3.6        MIT                 sharp@0.35.4         Apache-2.0      styled-jsx@5.1.6  MIT
--- 18 packages: {"MIT":8,"LGPL-3.0-or-later":2,"Apache-2.0":5,"Apache-2.0 AND LGPL-3.0-or-later AND MIT":1,"ISC":2}
```
After:
```
@next/env@16.3.6	MIT
@swc/helpers@0.5.23	Apache-2.0
client-only@0.0.1	MIT
next@16.3.6	MIT
react-dom@19.3.0	MIT
react@19.3.0	MIT
styled-jsx@5.1.6	MIT
--- 7 packages: {"MIT":6,"Apache-2.0":1}
```
A stricter file-level scan of the whole `.next/standalone` tree (1 105 files, including Next's vendored
`next/dist/compiled/*`):
```
files whose path mentions sharp/libvips/@img:        (none)
LICENSE/COPYING/NOTICE files mentioning GPL/LGPL:    (none)
native *.node binaries:                              (none)
vendored next/dist/compiled/* package licenses:      44 MIT · 2 Apache-2.0 · 2 BSD-3-Clause · 2 ISC · 1 BlueOak-1.0.0 · 1 CC0-1.0
```
Every one of these is permissive. The vendored set is the same as before because it is part of `next` itself.

### Item 1: standalone runtime proof (`NODE_ENV=production PORT=3100 node server.js`, runner layout)

```
/                                                  200 9679B text/html; charset=utf-8
/fonts/Vazirmatn-Regular.ttf                       200 122752B font/ttf
/fonts/Vazirmatn-Medium.ttf                        200 122656B font/ttf
/fonts/Vazirmatn-Bold.ttf                          200 123036B font/ttf
/_next/image?url=%2Ffonts%2Fx.png&w=64&q=75        404        (optimizer route disabled → sharp is never required)
/api/me/                                           404        (expected: T-001/T-003A not merged; Caddy routes it in compose)
<html lang="fa" dir="rtl" data-theme="light">
served Regular.ttf sha256 b69fd4c6…3a56 == public/fonts/Vazirmatn-Regular.ttf sha256 b69fd4c6…3a56
server log: only the 5 startup lines, no errors or warnings, during every run below
```
In Chromium, `/` loads in both themes with Vazirmatn loaded, **13 requests, 1× `/api/me/`, 0 third-party**
(output under item 3).

### Item 2: exact eslint pin + lockfile consistency

```
$ git diff frontend/package.json frontend/package-lock.json   (content lines only)
-    "eslint": "^9.39.5",          +    "eslint": "9.39.5",          (package.json)
-        "eslint": "^9.39.5",      +        "eslint": "9.39.5",      (package-lock.json, packages[""].devDependencies)
$ non-exact specs in package.json → none — all 12 direct deps exact
$ compare every lockfile entry except the root spec against main → resolved package tree identical to main
$ npm ci (fresh, above) → added 381 packages, exit 0 ; npm ls eslint → eslint@9.39.5
```
Note for the senior: `npm install --package-lock-only` on this lockfile adds 67 lines of metadata for 6 optional,
dev-only, `inBundle` packages inside Tailwind's wasm32 fallback (`@tailwindcss/oxide-wasm32-wasi/node_modules/{@emnapi/*,
@napi-rs/wasm-runtime,@tybys/wasm-util,tslib}`). A full `npm install` prunes those entries, so this is a difference
between npm's lock writers, not a change in resolution. I kept the **minimal one-line diff** that
`npm install --save-exact eslint@9.39.5` produced, and `npm ci` validates it.

### Item 3: W-01 without the header brand; toggle reachable; other routes intact

Chromium, production standalone server, 390×844 @2×:
```
W-01 light: {"theme":"light","lang":"fa","dir":"rtl","headerBrand":[],"toggleVisibleLabel":["تم تیره"],"ariaPressed":"false","toggleBox":{"x":16,"w":77,"h":44},"bodyBg":"rgb(245, 242, 234)","overflowX":false,"width":390,"h1":"حال‌من","vazirmatn":true}
  requests=13 /api/me/=1 third-party=0 []
W-01 dark: {"theme":"dark","lang":"fa","dir":"rtl","headerBrand":[],"toggleVisibleLabel":["تم روشن"],"ariaPressed":"true","toggleBox":{"x":16,"w":88,"h":44},"bodyBg":"rgb(21, 27, 36)","overflowX":false,"width":390,"h1":"حال‌من","vazirmatn":true}
  requests=13 /api/me/=1 third-party=0 []
404 route light: {… "headerBrand":["حال‌من"], "toggleVisibleLabel":["تم تیره"], "toggleBox":{"x":16,"w":77,"h":44} …, "h1":"404"}
404 route dark:  {… "headerBrand":["حال‌من"], "toggleVisibleLabel":["تم روشن"], "toggleBox":{"x":16,"w":88,"h":44} …, "h1":"404"}
tab order: ["BUTTON:تغییر تم روشن و تیره","A:شروع گفت‌وگو","BUTTON:چطور کار می‌کند؟"]
after Tab+Enter on toggle: dark [ 'تم روشن' ] aria-pressed true stored dark
after reload: dark
after click: light [ 'تم تیره' ]
360px light: width=360 overflowX=false toggle={"x":16,"w":77,"h":44}
360px dark:  width=360 overflowX=false toggle={"x":16,"w":88,"h":44}
```
The server HTML shows the same result with no JS: on `/` the `<header>` holds only the toggle `<button>`, and on
any other route it holds `<span>حال‌من</span>` followed by the button. The ZWNJ (U+200C) is kept in the brand
string. The toggle stays at the inline-end edge (`x=16`, left in RTL) on every route. `/` uses `justify-end` so it
doesn't jump to the right when the brand is gone.

Screenshots (`reports/worker-b/img/`):

| File | Shows |
|---|---|
| `t002f-w01-light.png` / `t002f-w01-dark.png` | W-01 in light and dark: brand only as the page H1, toggle in the header |
| `t002f-w01-comparison.png` | wireframe W-01 · light · dark, side by side |
| `t002f-other-route-light.png` | a non-`/` route: header brand «حال‌من» + toggle kept |
| `t002f-prehydration-stored-dark.png` | stored-dark user with all JS chunks blocked: correct «تم روشن» label (optional fix) |

The earlier T-002 screenshots (`w01-*.png`) are kept unchanged as the "before" record.

**Design choice to confirm:** on `/` I removed only the brand text. The header bar (card background + bottom border)
stays so the toggle sits on the same bar on every route. The wireframe W-01 has no toggle at all, so the bar is the
least-surprising place for it. If you'd rather have a bar-less, floating toggle on `/`, it's a one-class change.

### Optional: pre-hydration toggle label, before/after

All `/_next/static/chunks/*.js` requests are aborted, so React never hydrates. The inline theme bootstrap still runs.
```
BEFORE main@90aa3a3 pre-hydration (JS chunks blocked=7) OS=light stored=dark: theme=dark visible toggle label=«تم تیره» expected=«تم روشن» WRONG (flash)
BEFORE main@90aa3a3 pre-hydration (JS chunks blocked=7) OS=dark stored=null:  theme=dark visible toggle label=«تم تیره» expected=«تم روشن» WRONG (flash)
AFTER  T-002F      pre-hydration (JS chunks blocked=7) OS=light stored=dark: theme=dark visible toggle label=«تم روشن» expected=«تم روشن» OK
AFTER  T-002F      pre-hydration (JS chunks blocked=7) OS=dark stored=null:  theme=dark visible toggle label=«تم روشن» expected=«تم روشن» OK
AFTER  T-002F      pre-hydration OS=light stored=null: theme=light visible label=["تم تیره"]  (OK)
```
The pre-hydration screenshot is **byte-identical** to the hydrated dark screenshot (`cmp` exit 0), so nothing
changes visually when hydration happens.

Theme matrix after hydration (OS × stored). Theme, visible label and `aria-pressed` are all consistent:
```
matrix OS=light stored=null:  theme=light label=تم تیره aria-pressed=false CONSISTENT
matrix OS=light stored=light: theme=light label=تم تیره aria-pressed=false CONSISTENT
matrix OS=light stored=dark:  theme=dark  label=تم روشن aria-pressed=true  CONSISTENT
matrix OS=dark  stored=null:  theme=dark  label=تم روشن aria-pressed=true  CONSISTENT
matrix OS=dark  stored=light: theme=light label=تم تیره aria-pressed=false CONSISTENT
matrix OS=dark  stored=dark:  theme=dark  label=تم روشن aria-pressed=true  CONSISTENT
```
Remaining nit: before hydration, `aria-pressed` is still the server value (`false`). Screen readers use the stable
`aria-label` «تغییر تم روشن و تیره», and the state is corrected on hydration, which takes under a second.

### Item 4: README

```
## Environment

- `ALLOWED_DEV_ORIGINS` (**dev-only**, optional): comma-separated hostnames `next dev` accepts for
  cross-origin `/_next/*` requests, e.g. a proxied preview host. Read in `next.config.ts`; ignored by
  `next build` / the standalone server, never set in `.env.example`, compose or production.
```
I checked the claim: Next reads `allowedDevOrigins` only in `server/lib/router-utils/block-cross-site-dev.js`, which
is dev-server code. `grep -rn ALLOWED_DEV_ORIGINS` matches only `next.config.ts`, the README and the docs.

### Other checks

```
$ grep -rn "fonts.googleapis\|http://\|https://" frontend/app frontend/components frontend/lib
exit=1 (1 = no match)
$ grep -rn "next/image\|<img" frontend/app frontend/components frontend/lib
exit=1 (1 = none)   → images.unoptimized changes no current behaviour
```

### Docker-gated evidence: **MISSING, not passing**

This sandbox has no Docker engine (B-01). Here are the verbatim results from a scratch copy:
```
$ make verify-ports
json.decoder.JSONDecodeError: Expecting value: line 1 column 1 (char 0)
/bin/sh: 1: test: =: unexpected operator
FAIL: published ports != 1
make: *** [Makefile:22: verify-ports] Error 1
$ make size
FAIL: docker is required
make: *** [Makefile:24: size] Error 1
$ make check
docker compose run --rm api sh -c "python -m pytest -q"
/bin/sh: 1: docker: not found
make: *** [Makefile:19: check] Error 127
```

| Gate | Status |
|---|---|
| `docker compose build web` | **missing evidence** (no engine) |
| `make size` (web ≤ 250 MB) | **missing evidence**. Proxy only: runner payload 17.9 MB, estimated image ≈148 MB |
| `make verify-ports` | **missing evidence** via make. A docker-free static read of `docker-compose.yml` finds one `ports:` (proxy) and `expose:` only for web/api/db. T-002F changes no infra file (`git diff --name-only origin/main -- Makefile docker-compose*.yml docker Caddyfile .env.example` → 0 files) |
| `make check` | **missing evidence** (docker + backend not merged). Its frontend half (`npm run build`) plus lint, typecheck and check:tokens is green above |
| coverage (`engine/` / backend) | n/a: frontend-only task |

## Scope checklist (T-002F order)

1. ✅ `images.unoptimized` + `outputFileTracingExcludes` for `sharp`/`@img`. Evidence: `du` before/after, license
   scan (only permissive), standalone serves `/` + fonts, build green.
2. ✅ `eslint` exact `9.39.5`, lockfile consistent, `npm ci` clean.
3. ✅ No app-header brand on `/`. `<ThemeToggle/>` reachable (first tab stop, 44 px). Other routes keep the header
   brand. Light and dark screenshots.
4. ✅ `frontend/README.md` documents the dev-only `ALLOWED_DEV_ORIGINS`.
   Optional: ✅ pre-hydration toggle-label flash removed. No other changes.

## Rule compliance

- [x] scope = T-002F only (6 frontend files + 1 new component, report, screenshots, my STATUS row + appends)
- [x] no new deps. `images`/`outputFileTracingExcludes` are Next config knobs, not packages. The browser tooling
  lives in `/tmp`, outside the repo.
- [x] no secrets; no `.env` added (the `make` transcripts ran in a scratch copy)
- [x] palette/tokens untouched (`check:tokens` green); no hex outside `tokens.css`; RTL + both themes verified
- [x] safety §C unaffected: no copy/prompt/engine change. `<CrisisLayer/>` is still mounted in the root layout on
  every route. No diagnosis/therapist wording added.
- [x] never pushed to `main`, never touched worker-a's branch (`arena/01a0dc32-haleman`) or backend code

## Blockers / questions for senior

- **B-01 (again):** no Docker in this session. Web image size, `verify-ports` and `make check` need the Docker-capable
  run (Q-01 / T-004 amendment d).
- **Observation, not fixed (out of scope):** unknown routes render Next's built-in 404, which injects its own
  `body{background:#fff}` / dark `#000` style with English copy and no Vazirmatn. This has been the case since T-002
  (there's no `app/not-found.tsx`). The header on that route is correct. Posted on the message board. It could be a
  one-file follow-up (fa `not-found.tsx` on tokens) in a task of your choice.
- Please confirm or override the header-bar choice on `/` described under item 3.

## Ready for next task: yes

T-002F is pushed and reported. T-003B (`w-b/T-003B-consent-screen`, pre-authorised once T-002F is pushed + reported)
will run in a **separate** worker-b session, per supervisor instruction. This session stops here.
