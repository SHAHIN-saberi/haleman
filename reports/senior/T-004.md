# Task report — T-004: compose integration + `make check` + M1 acceptance (+ amendments a–f)

- Author: solo senior · Date: 2026-09-26 · Branch: `arena/01a0dcf9-haleman`
- Milestone: M1 (Foundations), last task · Order: `reports/senior/M1-orders-1.md` §T-004 + the amendments approved
  2026-09-25 (a–f). Plan gate approved by the owner («تایید», 2026-09-26).
- Acceptance (order §T-004): fresh clone → `make up` → `make check` → `make verify-ports` → `make size` →
  `scripts/m1-acceptance.sh`, all green, with `docker compose ps` showing proxy/web/api/db healthy.

## What I did

| Item | File | What changed |
|---|---|---|
| F5 test stage | `docker/Dockerfile.backend` | `FROM final AS test` — the prod layer is untouched, the test stage adds `requirements-dev.txt` as root and returns to `appuser`. |
| test override | `docker-compose.test.yml` **(new)** | `api.build.target: test`, `image: haleman-api-test`, **no `ports:` at all**; same project (`name: haleman`) so it reuses the real `db`. |
| Q-05 | `docker/Dockerfile.frontend` | both stages `node:20-alpine` → **`node:22-alpine`**. |
| a | `backend/.dockerignore`, `frontend/.dockerignore` **(now real files)**, `docker/.dockerignore.*` **deleted** | Real ignore files in their build contexts. The backend one deliberately keeps `tests/`, `pytest.ini`, `ruff.toml` (≈40 KB) because the test stage runs in that image; the frontend one keeps `public/fonts/*.ttf`. |
| b | `Makefile` | `make check` is now the single gate: `env` → `docker compose build` → test image build → `test-backend` → `npm ci`, `lint`, `typecheck`, **`check:tokens`**, `build` → `verify-ports`. Every Docker target depends on `require-docker`, which exits **2** (MISSING evidence, never a pass) when Docker is absent. `test-smoke` removed — it pointed at `npm run test:smoke`, which does not exist until T-024. New `make acceptance`. |
| b | `backend/scripts/backend-gate.sh` **(new)** | The in-image backend gate in one place: `ruff check .`, then `pytest -q --cov=. --cov-fail-under=80`, then — only once `engine/` has real modules — `coverage report --include='engine/*' --fail-under=90`. Called by `make test-backend` *and* by `scripts/docker-gates.sh`, so local and CI can't drift. |
| c | `scripts/init-env.py` **(new)**, `Makefile`, `backend/config/settings.py`, `tests/test_settings.py` | `make env` now writes `.env` (mode 0600) with `secrets.token_urlsafe` values for `DJANGO_SECRET_KEY`/`POSTGRES_PASSWORD` and copies every other line of `.env.example` verbatim; an existing `.env` is never touched (also guarded inside the script). With `DJANGO_DEBUG=0` a `change-me…` secret or DB password now raises `ImproperlyConfigured` naming the variable and pointing at `make env`; `DJANGO_DEBUG=1` still accepts them. 4 new tests. |
| d | evidence backlog | carried: this report records the Docker gates as MISSING (B-01) and hands the owner the exact local spot-run (below). `scripts/docker-gates.sh` gate 8 already calls `scripts/m1-acceptance.sh`, so the scripted gate and the manual one are the same checks. |
| order §4 | `scripts/m1-acceptance.sh` **(new)** | Six HTTP checks through the published port with a cookie jar: `/` 200 + `dir="rtl"`; `/api/health/` 200 without `Set-Cookie`; `/api/me/` issues `hid` (HttpOnly, SameSite=Lax, Max-Age=31536000) with `is_new=true`; `/api/chat/` 403 `consent_required`; `POST /api/consent/` 201; `/api/chat/` 200; second `/api/me/` `is_new=false` + `consent.informed=true`. 23 individual checks. Exit 0/1/2 (pass / fail / nothing reachable = MISSING). Bodies are JSON-parsed, never string-matched. |
| workflow | `.github/gates.workflow.yml` | `make check` is a real gate now: `continue-on-error: true` removed, job name quoted (YAML `:` rule) and renamed. |

No product, design, backend-business-logic or frontend-source change; `main` untouched.

## Evidence — reproduced here (real output)

| Gate | Command | Result |
|---|---|---|
| backend tests + coverage | `pytest -q --cov=. --cov-fail-under=80` (real Postgres 16.2) | **51 passed**, **96.43 %** (was 47 / 96.46 %) ✔ |
| new settings tests | `pytest tests/test_settings.py -q` | 9 passed ✔ (placeholder refused with `DEBUG=0`, allowed with `DEBUG=1`) |
| lint | `ruff check .` | `All checks passed!` ✔ |
| Django | `manage.py check` / `makemigrations --check --dry-run` | no issues / `No changes detected` ✔ |
| frontend | `lint`, `typecheck`, `check:tokens`, `build` | clean; `OK: tokens 1:1 with palette.md, 21 files scanned, 0 stray hexes`; 5 static routes ✔ |
| YAML | `yaml.safe_load` on both compose files + the workflow | valid; `docker-compose.test.yml` shows `api: ports=[] target=test image=haleman-api-test`; workflow jobs `continue-on-error=False` ✔ |
| shell/python syntax | `bash -n` ×4 · `py_compile` | clean ✔ |
| Makefile | `make -n <target>` ×10 | every command renders; no target references `npm start` or `test:smoke` ✔ |
| `make env` | on a temp copy of `.env.example` | creates `.env` **0600** with a 64-char secret and 32-char password, both different from the example, other keys byte-identical; second run prints "already exists — left untouched"; the script never prints a secret ✔ |
| placeholder guard | `DJANGO_SECRET_KEY=change-me-50-chars-min POSTGRES_PASSWORD=change-me DJANGO_DEBUG=0 python -c "import config.settings"` | raises `ImproperlyConfigured: DJANGO_SECRET_KEY still has the example value … Run 'make env'`; with `DJANGO_DEBUG=1` it imports fine ✔ |
| MISSING path | `bash scripts/docker-gates.sh` with no engine | prints the MISSING notice and exits **2** ✔ (not a pass) |
| MISSING path | `bash scripts/m1-acceptance.sh` with nothing listening | prints "nothing answers at http://localhost:80" and exits **2** ✔ |

### The acceptance script against a live stack (real services, no Docker here)

Django (gunicorn :8123, real Postgres 16.2) + the production **standalone** Next build (:3000) behind a one-port
proxy (:3300) — the same topology Caddy builds in the compose file:

```
M1 acceptance — base: http://127.0.0.1:3300
ok GET / -> 200 · ok html carries dir="rtl" · ok GET /api/health/ -> 200 · ok health body ok=true
ok health issued no cookie · ok GET /api/me/ (first visit) -> 200 · ok anonymous=true · ok is_new=true
ok consent.informed=false before accepting · ok device cookie issued · ok cookie is HttpOnly
ok cookie is SameSite=Lax · ok cookie Max-Age=31536000 · ok GET /api/chat/ without consent -> 403
ok error code=consent_required · ok POST /api/consent/ -> 201 · ok consent echo kind=informed
ok consent echo version=v1 · ok GET /api/chat/ with consent -> 200 · ok chat placeholder body
ok GET /api/me/ (returning) -> 200 · ok is_new=false on the second visit
ok consent.informed=true after accepting
M1 ACCEPTANCE: PASS (23 checks)                       ← exit 0
APP_PORT=3300 make acceptance → same PASS (Makefile wiring)
BASE=http://127.0.0.1:3000 bash scripts/m1-acceptance.sh
M1 ACCEPTANCE: FAIL (3 passed, 20 failed)             ← exit 1: the gate is not vacuous
```

## MISSING evidence (this environment, B-01 — not a pass)

| Item | Why |
|---|---|
| `make build` image builds, `make up`, healthchecks, `docker compose ps` (4/4 healthy) | no Docker engine in this sandbox |
| `make size` real numbers (api ≤ 350 MB, web ≤ 250 MB) | needs built images |
| `make test-backend` / `scripts/backend-gate.sh` inside `haleman-api-test` | needs the image |
| `scripts/m1-acceptance.sh` through Caddy on the published port | needs the stack |
| `docker compose -f docker-compose.yml -f docker-compose.test.yml config` | needs the compose CLI |

**Your one-line local spot-run** (from a fresh clone, Docker Desktop running):

```bash
make up && make check && make verify-ports && make size && make acceptance && docker compose ps
```

Expected green signals, in order: `make check` → `All checks passed!` + `51 passed` + `Required test coverage of 80%
reached. Total coverage: 96.43%` + `OK: tokens 1:1 …` + `OK: make check green`; `make size` → two lines under budget
+ `OK: image sizes within budgets`; `make acceptance` → `M1 ACCEPTANCE: PASS (23 checks)`; `docker compose ps` →
proxy/web/api/db **healthy**. If any step exits 2, that is MISSING (no Docker), not a pass. `bash
scripts/docker-gates.sh` runs the same set and writes raw evidence to `reports/docker-gates/<sha>/` (gitignored).

## Deviations / notes

- **D-1 `test-smoke` removed** from the Makefile rather than left broken: the target called `npm run test:smoke`, a
  script that does not exist yet. T-024 adds both back together.
- **D-2 `backend/scripts/backend-gate.sh`** is new (not in the order's file list): the alternative was duplicating
  the pytest/coverage flags in the Makefile *and* `docker-gates.sh`. One definition, both callers.
- **D-3 `.dockerignore` keeps `tests/`** in the backend build context, so the prod layer and the test stage share
  one context. Cost: ≈40 KB of source in the api image. The alternative (a second context) would let the tested
  image drift from the shipped one.
- **D-4 `make env` no longer copies `.env.example` byte-for-byte**: two values are generated, the rest is verbatim
  (comments included). With `DJANGO_DEBUG=0` the example values would refuse to boot anyway (amendment c), so the
  copy-and-run path had to change for `make up` to work at all from a fresh clone.
- **D-5 migrate-on-boot race under `--scale api=N`** recorded as a known M4/T-025 item (order §T-004-3), not fixed
  here.
- Repo state note: a sandbox re-provisioning wiped `/home/user/.cache` mid-task; the working tree was re-anchored
  onto branch head `33fe7ff` (`git add -A` + `git reset --soft`) and the toolchain rebuilt, with the staged diff
  re-checked to contain exactly this task's files.

## Rule compliance

- [x] scope: infra + scripts + one settings guard + tests + docs; no product/design/tech file, no frontend source
- [x] no new dependency (the placeholder guard is stdlib; `pyyaml` was installed only in my out-of-repo venv to
      validate YAML — it is in no project file)
- [x] no secret in the repo; generated `.env` is gitignored and 0600; the gate script never prints secret values
- [x] §C/§D untouched (no UI, no copy, no LLM call)
- [x] branch discipline: `main` untouched, nothing pushed outside `arena/01a0dcf9-haleman`
- [x] MISSING reported as MISSING (exit 2), never as a pass

## Ready for next: M1 milestone gate — `reports/senior/M1-final.md` (per `tech/tests.md` §B) + the owner's verdict;
then M2/T-005.
