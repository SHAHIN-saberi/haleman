# Worker report — T-001 Django project + health + device token

- Author: worker-a
- Date: 2026-09-26
- Branch: `arena/01a0dc32-haleman` (Arena session branch — **explicit alias of the ordered `w-a/T-001-django-skeleton` per decision D-S7**; the harness pins the session branch, no `w-a/*` branch may be created)
- Milestone: M1 — Foundations
- Based on: `main` @ `90aa3a3` (PR #5 merged — round-2 orders), order in `reports/senior/M1-orders-1.md` §T-001 (unchanged in round 2)

## What I did (short)

Implemented the M1 Django skeleton exactly per the order: `config/` project with 100 % env-driven
settings (Postgres-only, no sqlite fallback), `apps/accounts` with the `AnonymousIdentity` model +
`resolve_or_issue` device service + DRF `DeviceAuthentication`, `GET /api/health/` and `GET /api/me/`
per the binding §1 contract, empty pure-Python `engine/` package with a purity guard test, fully
pinned `requirements.txt` (F6), pytest harness, migrations. Only T-001 scope + its tests; no
Makefile/compose/Dockerfile/Caddyfile/frontend changes (`.env.example` append only, as pre-approved).

## Files changed

```
.env.example                                   # +CSRF_TRUSTED_ORIGINS (append only — the one var T-001 was ordered to add)
backend/manage.py
backend/requirements.txt                       # prod-only, fully pinned incl. transitive (F6)
backend/requirements-dev.txt                   # -r requirements.txt + pytest/pytest-cov/pytest-django/ruff
backend/pytest.ini
backend/ruff.toml                              # minimal lint config (migrations exempted from style)
backend/config/__init__.py
backend/config/settings.py
backend/config/urls.py
backend/config/wsgi.py
backend/apps/__init__.py
backend/apps/accounts/__init__.py
backend/apps/accounts/apps.py
backend/apps/accounts/models.py                # AnonymousIdentity
backend/apps/accounts/device.py                # resolve_or_issue / set_hid_cookie (order §5)
backend/apps/accounts/authentication.py        # DeviceAuthentication + DeviceCookieMixin (order §5)
backend/apps/accounts/views.py                 # health + MeView
backend/apps/accounts/migrations/0001_initial.py
backend/engine/__init__.py                     # empty pure-Python package (order §1/§7)
backend/tests/...                              # conftest, test_health, test_device_me, test_settings, test_engine_purity
STATUS.md                                      # own board row + append-only entries
reports/worker-a/T-001.md                      # this report
```

Nothing else. `git diff --stat` vs `90aa3a3` on tracked files: `.env.example +3`.

## How to verify (commands + expected output)

On a host with Docker (supervisor spot-run, T-004 scope):

```text
make check          # compose build + pytest + frontend build + verify-ports
make size           # haleman-api ≤ 350 MB (evidence BLOCKED here, see below)
```

Without Docker (what I ran, all reproducible):

```bash
python3 -m venv /tmp/v && /tmp/v/bin/pip install -r backend/requirements-dev.txt
cd backend
export DJANGO_SECRET_KEY=... DJANGO_DEBUG=0 POSTGRES_DB=haleman POSTGRES_USER=... \
       POSTGRES_PASSWORD=... POSTGRES_HOST=<host-or-socket-dir> POSTGRES_PORT=5432 COOKIE_SECURE=0
python manage.py migrate --noinput
python -m pytest --cov=. --cov-fail-under=80 --cov-report=term-missing
ruff check .
```

Expected: migrate applies `accounts.0001_initial`, 19 tests pass, coverage ≥ 80 %, "All checks passed!".

## Test results (paste real outputs)

First-report Docker availability statement (order §0):

- `docker version`:
  ```text
  /bin/bash: line 1: docker: command not found
  ```
- `docker compose version`:
  ```text
  /bin/bash: line 1: docker: command not found
  ```

→ Same blocker family as B-01/B-03 (no sandbox on this team has a Docker engine).
**`docker compose build api` output and the api image size (≤ 350 MB) could NOT be produced here** —
they remain **missing evidence**, not passing evidence. They are already carried by the round-2 review
into T-004's Docker-capable acceptance; request: same for T-001 (supervisor spot-run of
`docker compose build api` + `make size` once T-004's stack builds, or earlier if preferred).

### F6 — fully pinned requirements, clean-venv proof (order §2)

```text
$ python3 -m venv /tmp/venv-clean && /tmp/venv-clean/bin/pip install --no-deps -r backend/requirements.txt
$ /tmp/venv-clean/bin/pip check
No broken requirements found.          # rc=0
$ /tmp/venv-clean/bin/python -c "import django, rest_framework, gunicorn, psycopg; print(django.get_version(), rest_framework.VERSION, psycopg.__version__)"
django 5.2.17 drf 3.16.1 psycopg 3.2.13
```

Pins (all pre-approved in M1-orders-1.md §0; licenses: Django BSD-3, DRF BSD-3, gunicorn MIT,
psycopg/psycopg-binary LGPL-3.0 unmodified library pending Q-04):
`asgiref==3.12.1 Django==5.2.17 djangorestframework==3.16.1 gunicorn==23.0.0 packaging==26.3
psycopg==3.2.13 psycopg-binary==3.2.13 sqlparse==0.6.0 typing_extensions==4.16.0`
Dev: `pytest==8.4.2 pytest-cov==7.0.0 pytest-django==4.11.1 ruff==0.14.5`.

### Real Postgres 16 (order: "pytest-django, real Postgres")

No Docker ⇒ Postgres 16.2 booted in-sandbox from the `pgserver` wheel (dev-harness tool only —
**not** a project dependency, nothing added to `requirements*.txt`):

```text
[pg] PostgreSQL 16.2 on x86_64-pc-linux-gnu, compiled by gcc (GCC 10.2.1 …)
[pg] created database haleman
```

```text
$ python manage.py migrate --noinput   (rc=0)
Operations to perform:
  Apply all migrations: accounts
Running migrations:
  Applying accounts.0001_initial... OK
```

### pytest + coverage (order §Tests, gate ≥ 80 %)

```text
$ pytest --cov=. --cov-fail-under=80 --cov-report=term-missing
...................                                                      [100%]
Name                                       Stmts   Miss  Cover   Missing
------------------------------------------------------------------------
apps/accounts/authentication.py               16      0   100%
apps/accounts/device.py                       25      0   100%
apps/accounts/migrations/0001_initial.py       6      0   100%
apps/accounts/models.py                       13      1    92%   27
apps/accounts/views.py                        15      0   100%
config/settings.py                            31      1    97%   35
config/urls.py                                 3      0   100%
tests/...                                            0   100%
engine/__init__.py                             0      0   100%
------------------------------------------------------------------------
TOTAL                                        295     14    95%
Required test coverage of 80% reached. Total coverage: 95.25%
19 passed in 0.51s
```

(`manage.py`/`config/wsgi.py` are 0 % — never imported by pytest; they are exercised by the live
gunicorn run below. `engine/` coverage n/a — empty package by order; the ≥ 90 % gate activates when
`engine/` has code.)

Test ↔ order mapping: first visit → 1 row + cookie flags exactly per §1 · revisit → same row,
count 1, `last_seen_at` advanced, `is_new=false` · garbage cookie AND well-formed-unknown cookie →
new row, never 401 · DB `token_hash` ≠ cookie and `== sha256(cookie)` (64 hex) · health → 0 rows +
no Set-Cookie (even with a cookie presented) + 405 on POST · missing/empty `SECRET_KEY` →
`ImproperlyConfigured` (subprocess-isolated) · defaults: DEBUG off, Postgres-only engine, fa/UTC,
`USE_TZ`, `SECURE_PROXY_SSL_HEADER`, no admin/sessions/CSRF middleware · `Secure` flag follows
`COOKIE_SECURE` · engine purity guard (+ mutation test proving the guard has teeth) · `/api/me/`
returns exactly `{"anonymous","is_new"}` (locks T-001 scope until T-003A adds `consent`).

### ruff (order §Tests: "ruff check clean")

```text
$ cd backend && ruff check .
All checks passed!

$ grep -rn "django\|rest_framework" engine/   # purity cross-check
(no matches, rc=1)
```

### Live prod-path smoke (supplements the blocked compose run)

The pinned prod server (gunicorn) + Postgres 16 + real migrations, curl on the wire:

```text
=== GET /api/health/ ===
HTTP/1.1 200 OK
Content-Type: application/json
{"ok":true}                                  ← no Set-Cookie header at all (F9)

=== GET /api/me/ (first visit) ===
HTTP/1.1 200 OK
Set-Cookie: hid=L6xcHQf7tEQQg3LRtTB76_vy2BIvG6W8IOyMlDX5xuo; expires=…; HttpOnly; Max-Age=31536000; Path=/; SameSite=Lax
{"anonymous":true,"is_new":true}

=== GET /api/me/ (revisit with hid) ===
HTTP/1.1 200 OK                              ← no new Set-Cookie
{"anonymous":true,"is_new":false}

=== GET /api/me/ (garbage cookie) ===
HTTP/1.1 200 OK
Set-Cookie: hid=MhClYvlaLWU1qaqwaXPRg7m4TOP7DfyFTOiyUZdcawo; …; HttpOnly; Max-Age=31536000; Path=/; SameSite=Lax
```

DB cross-proof after that smoke (raw token never stored):

```text
haleman=# SELECT left(token_hash,12), length(token_hash) FROM anonymous_identities;
 320c324a74e4… | 64
$ sha256("L6xcHQf7tEQQg3LRtTB76_vy2BIvG6W8IOyMlDX5xuo") = 320c324a74e488ef…   ← matches the row
```

### Gates NOT run here (Docker-dependent, honest gap)

- `make check` (compose build + in-image pytest) — not runnable without a Docker engine.
- `docker compose build api` + api image size ≤ 350 MB — **missing evidence** (see above).
- `make verify-ports` / `make size` — untouched infra from T-000; still pending T-004 acceptance.
- `npm run check:tokens` (round-2 `make check` addition) — frontend untouched by T-001; worker-b's lane.

## Rule compliance

- [x] scope = this task only (`backend/**` + `.env.example` append + own STATUS rows + this report)
- [x] no new deps beyond the pre-approved list (all listed above with licenses; `pgserver` is a
      dev-harness tool **outside** the repo/deps, used only to run real Postgres in-sandbox)
- [x] no secrets (no `.env` committed; settings refuse to boot without `DJANGO_SECRET_KEY`;
      smoke keys are throwaway, used in ephemeral /tmp only)
- [x] safety section C unaffected: no prompts/copy/engine logic in T-001; no user-facing strings
      beyond contract JSON; risk screener n/a yet (M2/M3 tasks)
- [x] cookie contract exactly per §1/D-S6: HttpOnly, SameSite=Lax, Path=/, Max-Age=31536000,
      `Secure` from `COOKIE_SECURE`, sha256-only storage, health never issues tokens
- [x] D-S7 honored: session branch used as the ordered branch's explicit alias (this report + board row)

## Blockers / questions for senior

1. **Docker (B-01 family):** `docker compose build api` output + api image size (≤ 350 MB) are
   **not produced** — no Docker engine in this sandbox. Request: fold T-001's image evidence into
   T-004's Docker-capable acceptance (or a supervisor spot-run).
2. (informational) Tests ran on Postgres **16.2** via a wheel-bundled server — same major as
   compose's `postgres:16-alpine`. Senior may re-run on the compose DB at review.

## Ready for next task: yes

`/api/me/` already speaks the §1 shape; T-003A mounts `DeviceAuthentication`/`DeviceCookieMixin`,
adds `apps/summaries`, `HasInformedConsent`, `/api/consent/`, `/api/chat/` 403 gate, and extends
`/api/me/` with `consent` — no rework of T-001 files needed beyond the ordered additions.
Stopping here per protocol: T-003A starts only after T-001 is merged.
