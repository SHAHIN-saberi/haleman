# src/ — built by the build agent

Empty by design. The build agent creates the Next.js app here per `tech/tasks.md`
(start at T-001). Target tree is specified in `tech/architecture.md`.
Supervisors review per milestone via preview deploys.
