/**
 * CrisisLayer — W-10 overlay slot, mounted in the root layout so it exists on
 * every route (product rule 2; `tech/tests.md` §C.2).
 *
 * T-002 scope: empty placeholder, renders nothing. T-015 builds the overlay
 * (empathy + 115 call button + flow pause); whether M1/M2 show an interim 115
 * link is Q-07 (senior recommendation: yes once a public preview exists).
 */
export default function CrisisLayer() {
  return null;
}
